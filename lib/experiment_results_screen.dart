import 'package:flutter/material.dart';
class ExperimentResultsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Experiment Results Screen'),
    );
  }
}